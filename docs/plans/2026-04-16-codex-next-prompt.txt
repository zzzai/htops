你现在接手 /root/htops 项目。

先读取 `docs/plans/2026-04-16-codex-session-handoff.json`，然后按顺序阅读：
1. `src/sync-orchestrator.ts`
2. `src/app/reporting-service.ts`
3. `src/service.ts`
4. `src/runtime.test.ts`

当前工作主线不是重新梳理历史问题，而是继续基于已有修复做生产链路验收和可观测性补强。

已知前提：
- scheduled execution 已拆为 sync lane / delivery lane
- alert-only 不再是最终完成态
- 数据补齐后 send-report 应自动升级补发正式日报
- 历史 alert-only 残留已清零
- worker 已重启
- 日志中已看到新的同步波次和 endpoint 1.4 的 synced 记录

你先做这几件事：
1. 验证重启后的 worker 是否持续按新逻辑运行
   - 看 `/tmp/hetang-scheduled-worker.log`
   - 看 `runtime.doctor()`
   - 看最近日报与 `send_status`
2. 判断 doctor 里 `scheduled poller lastRun` 偏旧是不是正常现象
   - 是否因为当前长轮次尚未结束
   - 是否因为 poller outcome 回写存在缺口
3. 如果链路本身已经稳定，优先补可观测性，不要重新大改主逻辑
   - `alert-only -> sent` upgrade telemetry
   - doctor / queue / scheduler 状态显式展示
4. 只有在确认存在真实缺口时，再做最小代码改动，并补测试

明确约束：
- 不要回退已有代码
- 不要把更多业务职责塞进 `src/runtime.ts`
- 不要重复修已经解决的问题
- 保持修改集中在 owner modules
- 用 `apply_patch` 修改文件
- 改前先给出当前状态、风险点、最小动作
- 改后必须跑对应测试再下结论

建议先跑的命令：
- `tail -n 120 /tmp/hetang-scheduled-worker.log`
- `npx vitest run src/runtime.test.ts src/sync-orchestrator.test.ts src/service.test.ts src/schedule.test.ts`
- 用 `runtime.doctor()` 看 scheduler / delivery / poller 状态
- 查 `mart_daily_store_reports` 最近 `send_status`

你输出时先给：
1. 当前状态确认
2. 还存在的风险
3. 下一步最小动作

如果没有发现新的真实问题，优先补 telemetry / doctor，不要再扩大战线。
