[Unit]
Description=Ensure iptables entrypoints for Docker
DefaultDependencies=no
After=local-fs.target
Before=docker.service htops-compose.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/htops-ensure-iptables
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
