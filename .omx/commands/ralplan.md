# ralplan

Purpose: turn an approved direction into an implementation plan.

For this repository:

- save durable plans in `docs/plans/`
- name files with `YYYY-MM-DD-topic-plan.md`
- include exact files to touch
- include the verification commands
- include restart commands when service behavior changes
- prefer incremental rollout over big-bang edits to compatibility facades
