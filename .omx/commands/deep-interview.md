# deep-interview

Purpose: clarify the real task before writing code.

Use this command shape when the request is ambiguous, architectural, or likely to create rework if executed too early.

For this repository:

- identify whether the request belongs to Hermes, htops, or the bridge seam
- identify whether the target is fast serving-query, runtime-render, or async analysis
- state assumptions explicitly if the user already signaled approval to proceed
- keep questions narrow; do not ask open-ended discovery questions unless necessary
